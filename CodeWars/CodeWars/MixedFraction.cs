using System;

public class Kata
{
    public static string MixedFraction(string s)
    {
        string[] fraction = s.Split('/');
        Console.WriteLine(fraction.Length);
        int numerator = fraction[0];
        int denominator = fraction[1];

        string result = "";
        ///10/3 = 
        
        
        if (numerator)


        return s;
    }
}